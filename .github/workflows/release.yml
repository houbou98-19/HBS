name: Build Release Package

on:
  workflow_dispatch:
    inputs:
      bump:
        description: 'Version bump type'
        required: false
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      new-version: ${{ steps.version.outputs.NEW_VERSION }}
    
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: dev
      
      - name: Get latest tag and bump version
        id: version
        run: |
          git fetch --tags
          
          LATEST=$(python3 << 'PYTHON'
          import subprocess
          tags = subprocess.check_output(['git', 'tag', '-l']).decode().strip().split('\n')
          tags = [t.lstrip('v') for t in tags if t]
          tags = [t for t in tags if t and t[0].isdigit()]
          tags.sort(key=lambda x: tuple(map(int, x.split('.'))))
          result = tags[-1] if tags else '1.0.0'
          print(result)
          PYTHON
          )
          
          IFS='.' read -r MAJOR MINOR PATCH <<< "$LATEST"
          
          case "${{ github.event.inputs.bump }}" in
            major)
              MAJOR=$((MAJOR + 1))
              MINOR=0
              PATCH=0
              ;;
            minor)
              MINOR=$((MINOR + 1))
              PATCH=0
              ;;
            *)
              PATCH=$((PATCH + 1))
              ;;
          esac
          
          NEW_VERSION="${MAJOR}.${MINOR}.${PATCH}"
          echo "NEW_VERSION=${NEW_VERSION}" >> $GITHUB_OUTPUT
      
      - name: Create git tag
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          
          TAG="${{ steps.version.outputs.NEW_VERSION }}"
          TAG=${TAG#v}
          
          git tag "$TAG"
          git push origin "$TAG"

  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      hbs-changed: ${{ steps.changes.outputs.hbs }}
      carousel-changed: ${{ steps.changes.outputs.carousel }}
    
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: dev
      
      - name: Detect what changed
        id: changes
        run: |
          git fetch --tags
          
          # Get all tags sorted
          TAGS=$(git tag --sort=-version:refname)
          PREV_TAG=$(echo "$TAGS" | head -1)
          
          if [ -z "$PREV_TAG" ]; then
            # No tags, check all files in repo
            CHANGED=$(git ls-files)
          else
            # Compare against previous tag
            CHANGED=$(git diff --name-only $PREV_TAG..HEAD)
          fi
          
          echo "Changed files:"
          echo "$CHANGED"
          
          # Always build both for now
          echo "carousel=true" >> $GITHUB_OUTPUT
          echo "hbs=true" >> $GITHUB_OUTPUT

  build-hbs:
    needs: [version, detect-changes]
    if: needs.detect-changes.outputs.hbs-changed == 'true'
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          pip install pyinstaller flask requests
      
      - name: Update version in config
        run: |
          python3 << 'PYTHON'
          import json
          with open('config.json') as f:
              config = json.load(f)
          config['version'] = '${{ needs.version.outputs.new-version }}'
          with open('config.json', 'w') as f:
              json.dump(config, f, indent=2)
          PYTHON
      
      - name: Build HBS
        run: |
          cd installer/linux/hbs
          pyinstaller hbs.spec --distpath ./dist --workpath ./build
          tar -czf hbs-${{ needs.version.outputs.new-version }}.tar.gz -C dist hbs/
          mv hbs-${{ needs.version.outputs.new-version }}.tar.gz ../../
      
      - name: Upload HBS Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ needs.version.outputs.new-version }}
          files: installer/hbs-${{ needs.version.outputs.new-version }}.tar.gz
          body: "HBS Backend v${{ needs.version.outputs.new-version }}"
          generate_release_notes: true
          token: ${{ secrets.GITHUB_TOKEN }}

  build-carousel:
    needs: [version, detect-changes]
    if: needs.detect-changes.outputs.carousel-changed == 'true'
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          pip install pyinstaller pyglet requests
      
      - name: Build Carousel
        run: |
          cd installer/linux/carousel
          pyinstaller carousel.spec --distpath ./dist --workpath ./build
          tar -czf carousel-${{ needs.version.outputs.new-version }}.tar.gz -C dist hbs-carousel/
          mv carousel-${{ needs.version.outputs.new-version }}.tar.gz ../../
      
      - name: Upload Carousel Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ needs.version.outputs.new-version }}
          files: installer/carousel-${{ needs.version.outputs.new-version }}.tar.gz
          body: "Carousel Frontend v${{ needs.version.outputs.new-version }}"
          generate_release_notes: true
          token: ${{ secrets.GITHUB_TOKEN }}